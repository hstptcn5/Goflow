# Hello Webhook Pack

Minimal Pack Format v1 example.

Validate it from the repository root:

```bash
goflow pack validate examples/packs/hello-webhook
```

This example does not include secrets, binary files, or executable plugins.
