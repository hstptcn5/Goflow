# Goflow GOAL Progress

This file tracks the repository against `GOAL.md`. Status values are `NOT_STARTED`, `IN_PROGRESS`, `BLOCKED`, or `DONE`.

| DoD Item | Status | Implementation Evidence | Test Evidence | Related Files | Blocker / Notes |
|---|---|---|---|---|---|
| 1. CLI and MCP use the common execution path | DONE | CLI/MCP use REST client and API trigger endpoints; no separate SQLite engine path found. CLI/MCP set trigger source headers consumed by the shared API trigger path. | `scripts/goal-smoke-test.ps1` passed CLI, MCP stdio, and MCP HTTP execution. | `internal/cli/cli.go`, `internal/client/client.go`, `internal/mcpserver/server.go`, `internal/api/workflow_handler.go` | None. |
| 2. Workflow runs from UI, API, webhook, cron, CLI, MCP | IN_PROGRESS | API/webhook/cron use `TriggerService`; CLI/MCP use REST. | `scripts/goal-smoke-test.ps1` passed API, CLI, MCP stdio, and MCP HTTP paths. | `internal/application/trigger_service.go`, `internal/api/workflow_handler.go`, `main.go` | UI and cron remain code-audited but not smoke-run in this pass due time-based/manual nature. |
| 3. Execution has ID, source, principal, audit | DONE | Execution columns and audit store exist; API now derives principal from auth context and ignores spoofed `X-Goflow-Principal`. | `TestTriggerServiceSyncStoresSourceMetadata`, `TestRequestPrincipalIgnoresSpoofedHeader`; `scripts/goal-smoke-test.ps1` audit check passed. | `internal/storage/schema.go`, `internal/storage/execution_store.go`, `internal/storage/audit_store.go`, `internal/api/workflow_handler.go` | None. |
| 4. Concurrent idempotency is safe | DONE | Unique index plus duplicate insert conflict recovery returns existing execution. | `TestTriggerServiceAsyncConcurrentIdempotency` with 20 goroutines passes. | `internal/engine/engine.go`, `internal/storage/execution_store.go`, `internal/application/trigger_service_test.go` | Async path covered; sync idempotency remains non-primary because create execution endpoint is async-only. |
| 5. Global, node, workflow, and MCP limits work | DONE | Global, node, and per-workflow `reject` semaphores exist; MCP HTTP limiter persists per token/principal across requests. | `TestExecuteWorkflowConcurrencyLimit`, `TestMaxParallelNodesPerExecution`, `TestTriggerServiceEnforcesPerWorkflowRejectConcurrency`, `TestHTTPClientLimitersPersistAcrossRequestsForSameToken`; related tests pass. | `internal/engine/engine.go`, `internal/mcpserver/server.go`, `internal/mcpserver/http.go` | Queue policy is intentionally not selectable until implemented. |
| 6. CLI/MCP exposure is enforced server-side | DONE | `TriggerService` rejects inactive workflows, CLI-disabled workflows for CLI source, and MCP-hidden/approval workflows for MCP sources. | `TestTriggerServiceRejectsInactiveWorkflow`, `TestTriggerServiceEnforcesCLIExposure`, `TestTriggerServiceEnforcesMCPExposureAndApproval`; `scripts/goal-smoke-test.ps1` passed CLI/MCP execution with exposed workflow. | `internal/application/trigger_service.go`, `internal/api/workflow_handler.go`, `internal/client/client.go`, `internal/cli/cli.go`, `internal/mcpserver/server.go` | None. |
| 7. Scoped token and workflow allowlist do not leak data | DONE | Scoped `workflow:list` filters by token allowlist and returns summary only, without `nodes_json`/`edges_json`. | `TestListWorkflowsWithScopedTokenReturnsAllowlistedSummaries` passes. | `internal/api/auth.go`, `internal/api/workflow_handler.go`, `internal/api/execution_handler.go` | Admin/web UI list remains full for editor compatibility. |
| 8. Input, header, log, and credential redaction | DONE | Webhook payload omits sensitive headers before execution input persistence; node output/error/event redaction helpers are used in engine. | `TestWebhookDoesNotPersistSensitiveHeaders`, `TestRedact...` tests pass; `go test ./...` passed. | `internal/engine/redact.go`, `internal/api/workflow_handler.go`, `internal/engine/engine.go` | MCP execution output still returns execution metadata/logs, but sensitive values are redacted by engine before persistence. |
| 9. Sub-workflow has no infinite recursion | DONE | Engine tracks sub-workflow stack/depth in context, blocks cycles and depth above `GOFLOW_MAX_SUBWORKFLOW_DEPTH`, and propagates child failure details to parent. | `TestSubWorkflowCycleFailsExecution`, `TestSubWorkflowDepthLimitFailsExecution`; engine tests pass. | `internal/engine/engine.go`, `internal/nodes/sub_workflow.go`, `internal/engine/engine_test.go` | Default depth is 5. |
| 10. CLI import/export round-trip is complete | DONE | CLI import/export preserves workflow graph and interface metadata including approval and concurrency policy. | `TestReadWorkflowFilePreservesInterfaceMetadata`, CLI tests pass. | `internal/cli/cli.go`, `internal/client/client.go` | Validate command still uses parser-level structural validation, not a full registry-backed validator. |
| 11. MCP stdio and HTTP run with official client | DONE | Uses official MCP Go SDK, stdio and Streamable HTTP handlers; dynamic tool idempotency uses `_goflow` control envelope. | `TestHTTPHandlerWorksWithSDKStreamableClient`, `TestDynamicWorkflowHandlerRunsWorkflow`; `scripts/goal-smoke-test.ps1` passed MCP stdio and HTTP smoke. | `internal/mcpserver/server.go`, `internal/mcpserver/http.go`, `scripts/mcp-smoke-test.mjs`, `scripts/mcp-http-smoke-test.mjs` | None. |
| 12. Concurrency, security, and migration tests pass | DONE | Added targeted tests for concurrency, security, migration idempotence, schema subset, and MCP limiter behavior. | `go test ./...` passed. | `internal/**`, `scripts/**` | None. |
| 13. Binary runs on Windows and Linux | IN_PROGRESS | Single Go binary with embedded UI; release scripts exist; Windows binary rebuilt locally and smoke tested. | `go build ./...` passed; `scripts/goal-smoke-test.ps1` passed on Windows local environment. | `main.go`, `static_embed.go`, `scripts/build-release.*`, `scripts/goal-smoke-test.ps1` | Linux clean-machine smoke still manual/pending. |
| 14. No Docker, Redis, or PostgreSQL required | DONE | Default DB is SQLite and no required Docker/Redis/Postgres runtime dependency is used. | Covered by build/test configuration implicitly. | `go.mod`, `config/config.go`, `internal/storage/db.go` | Optional Redis/Postgres nodes are integrations, not runtime requirements. |
| 15. Docs do not claim unimplemented features | DONE | Docs updated after tests to reflect current source/exposure/idempotency/MCP limiter/schema/sub-workflow behavior. | Docs updated after `go test`, `go vet`, `go build`, and GOAL smoke passed. | `README.md`, `CHANGELOG.md`, `ROADMAP_PROGRESS.md`, `RELEASE.md` | Unreleased changelog entries should be included in the next patch/beta release notes. |

## Current Priority Queue

1. Fix security/authorization leaks: principal spoofing, workflow list summaries, webhook header redaction.
2. Fix concurrent idempotency race.
3. Fix CLI/MCP source and exposure enforcement.
4. Fix MCP dynamic tool control envelope and HTTP per-client limiter persistence.
5. Audit/fix per-workflow concurrency and sub-workflow depth/cycle tests.
6. Run full Go checks and available smoke tests.
7. Update docs and release notes after tests pass.
